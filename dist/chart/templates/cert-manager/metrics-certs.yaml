{{- if and .Values.certManager.enable .Values.metrics.enable }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "upgrade-toolkit.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "upgrade-toolkit.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
    namespace: {{ .Release.Namespace }}
spec:
    dnsNames:
        - {{ include "upgrade-toolkit.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "upgrade-toolkit.namespaceName" $ }}.svc
        - {{ include "upgrade-toolkit.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "upgrade-toolkit.namespaceName" $ }}.svc.cluster.local
    issuerRef:
        kind: Issuer
        name: {{ include "upgrade-toolkit.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
    secretName: metrics-server-cert
{{- end }}
